{
  "name": "@roo/bildungsplan-importer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {"roo-bildungsplan-import": "dist/cli.js"},
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "export-plans": "node dist/cli.js export-plans",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {"node": ">=20"},
  "dependencies": {
    "ajv": "^8.17.1",
    "cheerio": "^1.0.0",
    "commander": "^12.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
